spring.application.name=JDEmcpPur
mcp.ai.mcp.server.name=JDEMCPPur
mcp.ai.mcp.server.version=1.0.0

# MCP Server Configuration
spring.ai.mcp.server.protocol=STREAMABLE
#spring.ai.mcp.server.streamable-http.mcp-endpoint=/mcp
spring.ai.mcp.server.stdio=false
spring.ai.mcp.server.type=sync
DANGEROUSLY_OMIT_AUTH=true

jde.api.base-url=http://localhost:8081/api

# Tiempo máximo en milisegundos para que un conector espere a que se complete una solicitud.
server.tomcat.connection-timeout=600000
# Tiempo máximo en milisegundos que el servidor esperará a que se lea la entrada del cliente.
server.tomcat.keep-alive-timeout=600000
